<?xml version="1.0" encoding="UTF-8"?>
<!--
  VULNERABLE CODE
  File: src/main/webapp/WEB-INF/web.xml
  Purpose: Servlet 3.0 deployment descriptor. Registers the Struts2
           FilterDispatcher on every URL path (/*). All HTTP requests —
           including the exploit POST to /upload.action — are processed
           through this filter, which invokes the vulnerable multipart parser.
  Time in attack timeline: Deployed before March 2017; active during the full
                            exploitation window (May–July 2017).
-->
<web-app xmlns="http://java.sun.com/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://java.sun.com/xml/ns/javaee
                             http://java.sun.com/xml/ns/javaee/web-app_3_0.xsd"
         version="3.0">

  <display-name>Struts2 CVE-2017-5638 Demo</display-name>

  <!-- Struts2 filter — intercepts all requests and dispatches to Actions -->
  <filter>
    <filter-name>struts2</filter-name>
    <filter-class>
      org.apache.struts2.dispatcher.ng.filter.StrutsPrepareAndExecuteFilter
    </filter-class>
  </filter>

  <filter-mapping>
    <filter-name>struts2</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>

</web-app>
